@{
    ViewData["Title"] = "Stock Levels";
    Layout = "~/Views/Shared/_InventoryLayout.cshtml";
}

<div class="page-header d-flex justify-content-between align-items-center flex-wrap gap-3">
    <div>
        <h1 class="page-title">Stock Levels</h1>
        <p class="page-subtitle">Monitor inventory stock levels across all products</p>
    </div>
    <button class="btn btn-pdf-export" onclick="exportTableToPDF('Stock Levels', 'stockTableBody', ['SKU','Product','Stock','Reorder','Level','Status'], [0,1,2,3,4,5])">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg> Export PDF
    </button>
</div>

<div class="grid" style="grid-template-columns: 1fr 2fr; gap: 1.5rem; margin-bottom: 1.5rem;">
    <div class="card fade-in">
        <div class="card-header">
            <div>
                <h3 class="card-title">Stock Distribution</h3>
                <p class="card-subtitle">Inventory overview</p>
            </div>
        </div>
        <div class="card-body">
            <div class="chart-container" style="height: 250px;">
                <canvas id="stockChart"></canvas>
            </div>
        </div>
    </div>

    <div class="card fade-in">
        <div class="card-header d-flex justify-content-between align-items-center">
            <div>
                <h3 class="card-title">Stock Inventory</h3>
                <p class="card-subtitle">All product stock levels</p>
            </div>
            <div class="d-flex gap-2 align-items-center">
                <input type="text" class="form-control form-control-sm" placeholder="Search products..." id="stockSearch" style="min-width: 180px;">
                <select class="form-select form-select-sm" id="stockFilter" style="min-width: 140px;">
                    <option value="">All Stock Levels</option>
                    <option value="instock">In Stock</option>
                    <option value="low">Low Stock</option>
                    <option value="out">Out of Stock</option>
                </select>
            </div>
        </div>
        <div class="card-body" style="padding: 0;">
            <div class="data-table-wrapper">
                <table class="data-table" id="stockTable">
                    <thead>
                        <tr>
                            <th>SKU</th>
                            <th>Product</th>
                            <th class="text-center">Stock</th>
                            <th class="text-center">Reorder</th>
                            <th>Level</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody id="stockTableBody">
                        <tr><td colspan="6" class="text-center py-4" style="color: var(--gray-400);">Loading stock levels...</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer d-flex justify-content-between align-items-center" id="invStockPagination"></div>
    </div>
</div>

@section Scripts {
    <script src="~/js/inventory.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('stockSearch');
            const stockFilter = document.getElementById('stockFilter');

            function filterStock() {
                StockLevels.filter(
                    searchInput?.value || '',
                    stockFilter?.value || ''
                );
            }

            if (searchInput) searchInput.addEventListener('input', filterStock);
            if (stockFilter) stockFilter.addEventListener('change', filterStock);
        });
    </script>
}
